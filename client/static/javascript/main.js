var /*app name*/ = angular.module('/*app name*/', ['ngRoute']);

myApp.config(function ($routeProvider) {
  $routeProvider
    .when('/', {
      templateUrl: '',
      controller: '',
      controllerAs: ''
    })
    .when('/', {
      templateUrl: '',
      controller: '',
      controllerAs: ''
    })
    .otherwise({
      redirectTo: '/'
    })
  })